import exifTag from 'dummy/utils/exif-tag';
import { module, test } from 'qunit';

module('Unit | Utility | exif tag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = exifTag();
  assert.ok(result);
});
